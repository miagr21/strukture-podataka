#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

typedef struct stog *point;

typedef struct stog {
	int number;
	point next;
}stog;

int push(point head) {
	point temp = malloc(sizeof(stog));
}


int main() {

	stog head = { NULL };

	return 0;
}